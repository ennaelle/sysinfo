name: CI
on:  
  workflow_dispatch:

jobs:
  system: 
    runs-on: ubuntu-latest
    container:
      image: ubuntu:24.04
    steps:
      - run: hostnamectl
      - run: ip -4 a
      - run: free -m
