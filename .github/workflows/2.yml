name: Compil
on: [push]

jobs:
  pytest:
    runs-on: ubuntu-latest
    name: "pytest"
    steps:
      - uses: davidslusser/actions_python_pytest@v1.0.1
        with:
          src: "script/mysystem"
          options: "-v"
